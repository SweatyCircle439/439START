{
  "name": "cmdp",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --compile application.ts --outfile=dist/application ; bun build --compile index.ts --outfile=dist/startmenu ; rm dist/application"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ini": "^4.1.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "canvas": "^3.2.3",
    "fzf": "^0.5.2",
    "glob": "^13.0.6",
    "ini": "^6.0.0",
    "sharp": "^0.34.5"
  }
}
